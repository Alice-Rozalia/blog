export const setIsAuthenticate = ({ commit }, data) => {
  commit('setIsAuthenticate', data)
}

export const setUser = ({ commit }, data) => {
  commit('setUser', data)
}

export const setToken = ({ commit }, data) => {
  commit('setToken', data)
}

export const delToken = ({ commit }, data) => {
  commit('delToken', data)
}

export const setSearchList = ({ commit }, data) => {
  commit('setSearchList', data)
}

export const setMusic = ({ commit }, data) => {
  commit('setMusic', data)
}

export const setArticle = ({ commit }, data) => {
  commit('setArticles', data)
}

export const setTotal = ({ commit }, data) => {
  commit('setTotal', data)
}